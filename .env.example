# Archivo de ejemplo `.env.example`
# Copia este archivo a `.env` y completa los valores antes de usar en tu entorno local o en Render.
# NO subas un `.env` con secretos reales al repositorio.

# Seguridad
SECRET_KEY=change-me-to-a-long-random-string
DJANGO_DEBUG=False
ALLOWED_HOSTS=localhost,myapp.onrender.com

# Base de datos (Postgres) - formato: postgres://user:password@host:port/dbname
# Si usas SQLite en local, puedes omitir DATABASE_URL y usar la configuración por defecto.
DATABASE_URL=postgres://postgres:password@db-host:5432/dbname

# Email (SMTP) - usar console backend para pruebas localmente
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=you@example.com
EMAIL_HOST_PASSWORD=your-email-password

# Static files
STATICFILES_STORAGE=whitenoise.storage.CompressedManifestStaticFilesStorage

# Opcional: módulo de settings si tu plataforma lo requiere
DJANGO_SETTINGS_MODULE=shopproject.settings
